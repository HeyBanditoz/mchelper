package io.banditoz.mchelper.commands.logic;

public enum CooldownType {
    /** Indicates this cooldown should be against each guild. */
    PER_GUILD,
    /** Indicates this cooldown should be against each user. */
    PER_USER
}
